说明：
1. 在 install feature-splatting-inria 库后，将 `/conf/compute_obj_part_feature.py` 替换 feature-splatting-inria 中的 compute_obj_part_feature.py
2. 将 weights 文件夹解压后放在 `GraspSplats/` 目录下，如 `/GraspSplats/weights/l2.pt`
3. 然后按照原文 Usage 使用即可